services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.astro
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
      # Add any other frontend environment variables here
    restart: unless-stopped
    volumes:
      - ./src:/app/src
      - /app/node_modules